# Text-to-Image-Generation
Text-to-Image Generation with Stable Diffusion (PyTorch) – Implements a pipeline using Hugging Face Diffusers to generate images from text prompts, with output handling via NumPy and PIL for saving results.

## Features
- Uses **Stable Diffusion v1.5** model from Hugging Face.
- CPU-compatible (can be switched to GPU if available).
- Handles multiple output formats from the pipeline.
- Saves generated images in PNG format.

## Requirements
- Python 3.8+
- PyTorch
- diffusers
- Pillow
- NumPy

To run the script
- python image.py
